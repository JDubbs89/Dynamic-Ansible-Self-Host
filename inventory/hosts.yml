all:
  hosts:
    homelab:
      ansible_host: YOUR_SERVER_IP  # Replace with your Oracle instance public IP
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/.ssh/id_rsa  # Replace with your SSH key path
      ansible_python_interpreter: /usr/bin/python3
